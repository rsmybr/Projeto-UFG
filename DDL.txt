Banco de Dados PostgreSQL: PATRIMONIO
Usuário: postgres
Senha: postgres

/* Instituição */
CREATE TABLE INSTITUICAO (CODINSTITUICAO SERIAL PRIMARY KEY,
                           DESCRICAO VARCHAR(100));

/* Local */
CREATE TABLE LOCAL (CODLOCAL SERIAL PRIMARY KEY, CODINSTITUICAO INTEGER, DESCRICAO VARCHAR(100), FOREIGN KEY (CODINSTITUICAO) REFERENCES INSTITUICAO);